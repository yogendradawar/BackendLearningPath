//#3

// NPM understanding

    // npm - node package manager
    // node js core mein jo installed aata hai wo kehlaata hai MODULES
    // npm se download krte hai Wo hota hai PAKAGES

// Installing and unistalling anything basics & advanced

    // install -> npm i packageKaNaam
    // uinstall -> npm uninstall pakageNaam
    // installing particular version

// understanding node_modules

    // dependencies - packages and packages ki dependencies
    // devdependencies - aise packages jo sirf development mein kaam aayege pr jab aap ban jayega aur upload ho jaayega tab hum in packages ko use nahi kar rahe honge 

    // QUESTION OF SCRIPTS -> 
    //     kai baar ap koi script chalaate ho and us script mein aap likhte  ho yeh     commands

    //     npm start 
            // npm test are pre installed in the system 
            // npm run OURFILE (which is created in the scripts by us so we have to put run in that command);

    //     npm run dev 
    //     npm run concurrent

    // scripts - understanding default scripts PATH and custom scripts

console.log("Help");